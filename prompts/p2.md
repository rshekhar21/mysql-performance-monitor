Implement the complete “Add Server” feature in the React frontend and connect it to the existing backend server-management API.

Current problem:
apps/web/src/features/servers/server-management-page.tsx renders an “Add Server” button, but the button has no onClick handler, no navigation, and no form.

Requirements:

1. Clicking “Add Server” must open a professional modal or drawer.
2. The form must include:
   - Server name
   - Host
   - Port, default 3306
   - Username
   - Password
   - SSL mode
   - Enabled status
3. Add a “Test Connection” button.
4. Test Connection must call the existing backend test-connection endpoint.
5. Show clear success or error feedback.
6. Add a “Save Server” button.
7. Save must call the existing POST monitored-server endpoint.
8. After successful save:
   - close the modal;
   - invalidate the TanStack Query server-list cache;
   - refresh the Servers table;
   - show a success message.
9. Validate all fields.
10. Disable buttons while requests are running.
11. Do not log or expose the password.
12. Reuse existing API client, validation schemas, query keys, UI components, and backend contracts.
13. Check the actual backend routes and request payloads before coding. Do not invent endpoints.
14. Add edit, test, enable/disable, and delete actions to existing server rows if backend support already exists.
15. Add tests for:
   - opening the form;
   - validation;
   - successful test connection;
   - failed test connection;
   - successful create;
   - cache invalidation and list refresh.
16. Run typecheck, tests, and production build.
17. Explain the root cause and list all files changed.